import { FullAdminAuthGuardGuard } from './full-admin-auth-guard.guard';

describe('FullAdminAuthGuardGuard', () => {
  it('should be defined', () => {
    expect(new FullAdminAuthGuardGuard()).toBeDefined();
  });
});
